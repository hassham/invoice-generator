Both `IG-4` and `IG-7` are unblocked now, but `IG-7` still has the real blocker noted last session (`invoice.invoices.customer_id` is `NOT NULL` with no customer find-or-create path — that's `IG-9` territory). `IG-4`'s first Story looks like it may already be satisfied by the existing app:

**`IG-28`** ("Create and preview an invoice anonymously") requires: all fields/templates available anonymously, live preview from anonymous input, no forced account-owned save. I checked `/invoice/create` — there's no session/auth check anywhere on that route (`grep` for auth hooks found nothing there), and no persistence exists yet at all, so nothing gets "saved" regardless. This mirrors `IG-41` last session: likely no new production code, just verification + explicit test coverage.

I want to:
1. Transition `IG-28` To Do → In Progress now (claim it, per the multi-agent convention).
2. Verify its three ACs hold against the current app, add explicit test coverage if any gap is found, then transition it → Done with a verification comment.
3. Continue into `IG-29` ("Recover an anonymous invoice draft locally") next — that one's real new work (localStorage draft persistence + recovery), staying in Epic `IG-4`.

Give me an explicit yes on the `IG-28` transitions (and I'll come back to you before transitioning `IG-29` or anything else) — or tell me if you'd rather I pivot to `IG-9` (customer records) first instead.
IG-28 is Done. Summary of what happened:

- Verified all three ACs already held in the existing app (no auth gate, live preview is pure client state, no invoice persistence layer exists at all).
- Added one test proving the anonymous flow leaves zero persistence side effects (`CreateInvoiceEditor.test.tsx`, commit `dc2eaac`); full suite passes 36/36.
- Committed locally only, per your standing preference — not pushed.
- Left a verification comment on IG-28 and transitioned it to Done.

Per the plan, I'll stop here before touching IG-29 ("Recover an anonymous invoice draft locally") — that's real new work (localStorage draft persistence), and continue_log.txt said to come back to you first. Want me to proceed into IG-29, or pivot to IG-9 (customer records) as the other option you'd flagged?
Picking up where the last session left off: `IG-28` is Done, and the plan was to continue into **`IG-29`** ("Recover an anonymous invoice draft locally") next, with `IG-9` (customer records) as the alternative — both are still unclaimed (`To Do`, no assignee) in Jira. I'll take "continue to next task" as going with the stated plan.

I want to:
1. Transition `IG-29` To Do → In Progress now (claim it).
2. Implement localStorage draft persistence + recovery for anonymous invoice creation.
3. Transition it → Done with a verification comment once tests pass.

Give me an explicit yes on the `IG-29` transition and I'll get started.
`continue_log.txt` appears to be locked by another process right now (likely in active use elsewhere), so the append didn't go through — I'll leave it rather than force it. The durable state is already recorded in Jira and git anyway.

**Summary:** IG-29 ("Recover an anonymous invoice draft locally") is now Done. Implemented `localStorage`-based auto-save/restore for the anonymous invoice editor with a 24h retention window, a restoration notice, and an explicit discard action — 16 new tests, full suite (278/278) passing, manually verified end-to-end in a real browser. Committed locally as `650f47e` (not pushed), and left a verification comment on the Jira ticket before transitioning it to Done.
